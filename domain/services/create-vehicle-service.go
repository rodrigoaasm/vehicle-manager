package services

func CreateVehicle(v_type string, id string, name string, cor string, serie string) error {
	// if v_type == "Car" {
	// 	vehicle := entities.NewCar(id, name, cor, serie)
	// } else if v_type == "Truck" {
	// 	vehicle := entities.NewTrunk(id, name, cor, serie)
	// } else {
	// 	errors.New("Type is invalid")
	// }

	return nil
}
